# Generated artifacts. Unlike the extracted Dafny sources elsewhere in this
# repository, everything here CAN be regenerated by CI from committed sources
# (`scripts/build.sh` runs `dotnet publish` and then clang++), so it is not
# committed.
build/
lib/
*.dylib
*.so
*.dll
*.dSYM/
